default:
    suites:
        core_features:
            paths:    [ %paths.base%/features/core ]
            contexts: [ CoreDomainContext ]
        cli_features:
            paths:    [ %paths.base%/features/cli ]
            contexts: [ CliContext ]
